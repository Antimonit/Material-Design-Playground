<resources xmlns:tools="http://schemas.android.com/tools">
    <item name="wrap_content" type="dimen">-2</item>
    <item name="match_parent" type="dimen">-1</item>

    <!-- Disable the extra top and bottom padding in buttons so that they fully fill bounds -->
    <dimen tools:override="true" name="mtrl_btn_inset">0dp</dimen>

    <dimen name="fab_margin">16dp</dimen>
    <dimen name="form_width">@dimen/match_parent</dimen>

    <!-- Same as Widget.MaterialComponents.BottomAppBar elevation attribute -->
    <dimen name="toolbar_elevation">8dp</dimen>
</resources>
